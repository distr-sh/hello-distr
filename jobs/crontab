# ┌───────────── minute (0-59)
# │ ┌───────────── hour (0-23)
# │ │ ┌───────────── day of month (1-31)
# │ │ │ ┌───────────── month (1-12)
# │ │ │ │ ┌───────────── day of week (0-6) (Sunday to Saturday)
# │ │ │ │ │
# * * * * * command

# Execute a local command (that is already in the image)
* * * * * echo "Hello world"

# Call the internal API of another container in the Docker network
* * * * * wget -O- http://backend:5000/health
