# Loaded automatically by `next dev`. Points the frontend at the backend on
# the host (local Flask process, or the deploy compose with the port override).
API_URL=http://127.0.0.1:5000
NEXT_PUBLIC_API_URL=http://127.0.0.1:5000
