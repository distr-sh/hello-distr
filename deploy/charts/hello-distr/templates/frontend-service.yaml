apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-distr.fullname" . }}-frontend
  labels:
    {{- include "hello-distr.component.labels" (dict "component" "frontend" "context" .) | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-distr.component.selectorLabels" (dict "component" "frontend" "context" .) | nindent 4 }}
