apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-distr.fullname" . }}-test-connection"
  labels:
    {{- include "hello-distr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-distr.fullname" . }}-backend:{{ .Values.backend.service.port }}/health']
  restartPolicy: Never
